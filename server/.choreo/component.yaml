# +required The configuration file schema version
schemaVersion: "1.2"

# +optional Incoming connection details for the component
endpoints:
  - name: temporary-chat-api
    displayName: Temporary Chat API
    service:
      basePath: /
      port: 3000
    type: REST
    networkVisibilities:
      - Public
    schemaFilePath: openapi.yaml
